<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xiewujie.cqupthand">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.xiewujie.cqupthand.tool.MyAppliction"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.CurriculumActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LogInActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.MineActivity" />
        <activity android:name=".activity.FindActivity" />
        <activity android:name=".activity.QAHomeActivity" />
        <activity android:name=".activity.QAQutionAnswer" />
        <activity android:name=".activity.LookPicActivity"></activity>
    </application>

</manifest>